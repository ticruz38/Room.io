type Query {
    room(id: String!): Room
    rooms: [Room]
    order(id: String!): Order
    user(id: String!): User
    login(login: UserLogin!): User
    tags: [String] ## all tags added to the room in the app
}
